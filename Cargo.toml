[package]
name = "maysh"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
gix = { version = "0.77.0", default-features = false, features = ["revision"] }
owo-colors = "4.2.3"

[lints.clippy]
uninlined_format_args = "warn"
