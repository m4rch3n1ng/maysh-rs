[package]
name = "maysh"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
gix = { version = "0.83.0", default-features = false, features = ["sha1", "revision"] }
owo-colors = "4.3.0"

[lints.clippy]
uninlined_format_args = "warn"
